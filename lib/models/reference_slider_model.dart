class ReferenceSliderModel {
  final String id, title, summary, icon;
  final bool hasLink;
  final int newsId;
  ReferenceSliderModel(this.id, this.title, this.summary, this.newsId, this.icon, this.hasLink);
}